appiVersion: v1
kind: ResourceQuota
metadata:
  name: computing-resource-quota
  namespace: raghadalrehaili-tawazun
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
